import cv2

if __name__ == '__main__':
    cap = cv2.VideoCapture(0, cv2.CAP_DSHOW)
    while True:
        ret, frame = cap.read()
        if not wpose = move_group.get_current_pose().pose
        wpose.position.x += 0.0
        wpose.position.y += 0.0
        wpose.position.z += 0.0
        waypoints.append(copy.deepcopy(wpose))
        